name: client

on:
  push:
    branches:
      - "*"

jobs:
  test-1:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Hello, world!" 

  test-2:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Hello, world!" 
